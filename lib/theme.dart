import 'package:flutter/material.dart';

class AppTheme {
  static const Color backgroundColor = Colors.white; // ✅ 배경색을 쉽게 관리하도록 변수 생성
  static const Color appBarColor = Colors.white; // ✅ 앱바 색상
  static const Color bottomBarColor = Colors.white; // ✅ 하단 바 색상
  static const Color textColor = Colors.black; // ✅ 기본 텍스트 색상
}
