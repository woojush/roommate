// 앱의 매칭 탭

import 'package:flutter/material.dart';

class Matching extends StatelessWidget {
  const Matching({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Matching Tab');
  }
}
